apiVersion: v1
kind: Service
metadata:
  name: {{ include "mintax-test-directory.fullname" . }}
  labels:
    {{- include "mintax-test-directory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mintax-test-directory.selectorLabels" . | nindent 4 }}
