apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mintax-test-directory.fullname" . }}-test-connection"
  labels:
    {{- include "mintax-test-directory.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mintax-test-directory.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
