apiVersion: v1
kind: Service
metadata:
  name: {{ include "mintax-worker.fullname" . }}
  labels:
    {{- include "mintax-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mintax-worker.selectorLabels" . | nindent 4 }}
