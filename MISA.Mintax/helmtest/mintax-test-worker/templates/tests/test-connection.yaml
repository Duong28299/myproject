apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mintax-worker.fullname" . }}-test-connection"
  labels:
    {{- include "mintax-worker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mintax-worker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
