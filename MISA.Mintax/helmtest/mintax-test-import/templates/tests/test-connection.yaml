apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "importexport.fullname" . }}-test-connection"
  labels:
    {{- include "importexport.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "importexport.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
