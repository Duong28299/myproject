apiVersion: v1
kind: Service
metadata:
  name: {{ include "importexport.fullname" . }}
  labels:
    {{- include "importexport.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "importexport.selectorLabels" . | nindent 4 }}
