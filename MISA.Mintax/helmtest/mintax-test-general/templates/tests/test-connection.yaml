apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mintax-test-general.fullname" . }}-test-connection"
  labels:
    {{- include "mintax-test-general.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mintax-test-general.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
