apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mintax-auth.fullname" . }}-test-connection"
  labels:
{{ include "mintax-auth.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "mintax-auth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
