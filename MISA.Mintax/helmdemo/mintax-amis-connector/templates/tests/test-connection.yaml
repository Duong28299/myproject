apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "amis-connector.fullname" . }}-test-connection"
  labels:
    {{- include "amis-connector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "amis-connector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
