apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mintax-ui.fullname" . }}-test-connection"
  labels:
    {{- include "mintax-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mintax-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
