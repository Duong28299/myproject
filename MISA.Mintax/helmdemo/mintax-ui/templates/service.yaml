apiVersion: v1
kind: Service
metadata:
  name: {{ include "mintax-ui.fullname" . }}
  labels:
    {{- include "mintax-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mintax-ui.selectorLabels" . | nindent 4 }}
