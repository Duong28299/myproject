apiVersion: v1
kind: Service
metadata:
  name: {{ include "mtax-connector.fullname" . }}
  labels:
    {{- include "mtax-connector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mtax-connector.selectorLabels" . | nindent 4 }}
