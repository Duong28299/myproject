{{- if .Values.etax.ingress.enabled -}}
{{- $fullName := include "mintax.fullname" . -}}
{{- $svcPort := .Values.etax.service.port -}}
{{- if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-etax
  labels:
{{ include "mintax.labels" . | indent 4 }}-etax
  {{- with .Values.etax.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
{{- if .Values.etax.ingress.tls }}
  tls:
  {{- range .Values.etax.ingress.tls }}
    - hosts:
      {{- range .hosts }}
        - {{ . | quote }}
      {{- end }}
      secretName: {{ .secretName }}
  {{- end }}
{{- end }}
  rules:
  {{- range .Values.etax.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
        {{- range .paths }}
          - path: {{ . }}
            backend:
              serviceName: {{ $fullName }}-etax
              servicePort: {{ $svcPort }}
        {{- end }}
  {{- end }}
{{- end }}
