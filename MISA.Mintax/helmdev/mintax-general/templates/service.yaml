apiVersion: v1
kind: Service
metadata:
  name: {{ include "mintax-test-general.fullname" . }}
  labels:
    {{- include "mintax-test-general.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mintax-test-general.selectorLabels" . | nindent 4 }}
