apiVersion: v1
kind: Service
metadata:
  name: {{ include "etax.fullname" . }}
  labels:
    {{- include "etax.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "etax.selectorLabels" . | nindent 4 }}
