apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mtax-connector.fullname" . }}-test-connection"
  labels:
    {{- include "mtax-connector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mtax-connector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
